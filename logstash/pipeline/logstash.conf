input {
  tcp {
    port => 5000
    codec => json
  }
  udp {
    port => 5000
    codec => json
  }
}

filter {
  if [type] == "api-gateway" {
    mutate {
      add_field => { "service" => "api-gateway" }
    }
  }
  if [type] == "user-service" {
    mutate {
      add_field => { "service" => "user-service" }
    }
  }
  if [type] == "billing-service" {
    mutate {
      add_field => { "service" => "billing-service" }
    }
  }
  if [type] == "integration-service" {
    mutate {
      add_field => { "service" => "integration-service" }
    }
  }
  if [type] == "task-worker" {
    mutate {
      add_field => { "service" => "task-worker" }
    }
  }

  date {
    match => [ "@timestamp", "ISO8601" ]
    target => "@timestamp"
  }
}

output {
  elasticsearch {
    hosts => ["elasticsearch:9200"]
    index => "logs"
  }
  stdout { codec => rubydebug }
} 